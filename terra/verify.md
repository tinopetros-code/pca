- ping from vm1 to vm2 via internal IP should work:
`ping -c 3 <Enter mynet-vm-2's internal IP here`

- ping from vm1 to vm2 via external IP should work:
`ping -c 3 <Enter mynet-vm-2's external IP here>`